# Paste into your local coding agent

You are working on the attached Alibi 0.2.0 publishing bundle. This is an existing working app, not a redesign prompt. Inspect the source, README, AGENTS.md, architecture, deployment instructions, test reports and release checklist.

First run the build and Node suites unchanged. Run the isolated Chromium UI suite. Then launch the actual local/hosted app in a normal browser and close the verification gap: IndexedDB saves surviving reload/restart, backup merge and replacement, legacy backup restoration, cross-tab conflicts, coherent offline restart and two-release service-worker updates. Use disposable test profiles and retain evidence. Mark anything not actually tested as pending.

Fix discovered defects before adding features. Preserve every published puzzle ID/revision, the alibi-device database identity, saved puzzle snapshots, non-destructive compatibility behaviour and explicit update activation. Keep app assets and saves separate. Do not put secrets into the client or replace this static app with an unnecessary backend.

Prepare a final browser-upload ZIP with index.html at its root, matching hashed assets, manifest, icons, service worker and security headers. Rebuild from source, test it, and provide build ID and checksums. Prepare a short human deployment checklist for the owner's chosen existing Cloudflare project. Do not guess a project, create paid resources, purchase domains or publish to an unconfirmed destination.

The first planned product improvements after acceptance are human puzzle calibration, better deduction-based hints and stronger authored mystery continuity. Current casebooks are anthologies; current reveals use stored solutions. Do not describe them as branching narratives or explainable reasoning hints unless you implement and test those capabilities.

Keep human-facing updates brief: what changed, evidence, remaining blocker and the single next required decision.
