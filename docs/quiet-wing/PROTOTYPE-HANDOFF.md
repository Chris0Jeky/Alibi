# Quiet Wing integration handoff

Work in the user's current Alibi repository, not an old source archive. The review anchor was `1f049a794609be04f29a91117fbd1a8701d28d08`; main may have advanced. Read AGENTS.md, docs/STATE.md, issue #11 and the current test/deployment contracts first.

## Instruction to the agent

Implement this laboratory as a bounded extension of Alibi. Preserve all existing puzzle IDs, saved revisions, Club rules, databases, assets and public origins. Compare the current repository with the supplied reference extension before editing. Do not apply the combined distribution over source files.

First reproduce the remaining affected-Android freeze script or preserve that item as a clearly unverified human gate. Port the realm/companion/classic/garden engines behind a lazy activity boundary. Retain pure reducers, separate manual state from derived presentation, and dispose timers/listeners on route exit. Integrate the Quiet Wing entry into the existing navigation instead of keeping the distribution-only bridge indefinitely.

Keep the renderer cheap and predictable. Do not add Three.js, physics, large textures, avatars, networking or accounts just to match a screenshot. The current mesh data and OBJ export leave a path to a later WebGL renderer. Add that only with a measured benefit and a phone budget.

Fetch the requested artwork using the supplied rights-checking tool, inspect its output, optimize it, keep the receipts, and ship selected images as a lazy/offline pack. Do not claim model packs or animation rigs are integrated until they actually render and pass interactions. Keep all CC0 files/receipts. Do not use the inspiration screenshots as distributable art.

Consolidate backup discovery without pretending restores across multiple databases are one transaction. Keep pre-restore recovery and staged validation. Preserve unknown future records and show a recoverable error. Implement a user-visible raw-recovery path rather than telling people to clear site data.

Run the existing repository gates, the supplied pure/contract tests, all new controls in a real browser origin, restart/offline/update/cross-tab checks, and physical Android/TalkBack where available. Clearly separate simulated tests from hosted/device evidence. Add small reviewable issues/commits for remaining gaps. Do not publish or replace the working origin without the owner's established authorization.

Human-facing copy: concise, literal, short sentences, no hype or em dashes. At the end report what works, exact evidence, remaining gates, deployment URL when actually published, and recovery instructions.

## Suggested issue boundaries

QW-01: lazy activity registry and source-native navigation integration. Acceptance: only the entered activity executes its renderer; route disposal proven; no root-SW interception.
QW-02: realm import/export contracts. Acceptance: invalid stack rejection, undo, real PNG/SVG/OBJ exports and OBJ importer inspection.
QW-03: pointer/keyboard/TalkBack plot controls. Acceptance: physical Android pinch/pan/place without accidental builds; accessible plot inspector.
QW-04: versioned save schema, timeout recovery and raw export. Acceptance: late transactions abort, CAS conflict, committed recovery copy, failed writes never labelled saved.
QW-05: original companion state machine and lifecycle. Acceptance: all species/actions, reduced motion, no hidden-tab animation leak, no state writes per animation frame.
QW-06: idle garden/stroll idempotency. Acceptance: reload before/after completion, rollback/forward clock tests, no double collect, no punitive decay.
QW-07: curated museum pack. Acceptance: public-domain receipts, hashes, optimized files, offline puzzle imagery and source credits.
QW-08: classic reasoning UX. Acceptance: every playable configuration solved through controls, illegal moves explained, multiple valid answers supported, human difficulty review.
QW-09: journal integration. Acceptance: unique source IDs, idempotent awards, imported/local status not submitted to trusted rankings, badges hidden in Zen.
QW-10: one backup entry point. Acceptance: manifest-driven export, staged restore/recovery, no false cross-DB atomicity claim.
QW-11: measured graphics envelope. Acceptance: representative phone on default/full scene, long-session memory, hidden-tab CPU and no recurring whole-world mesh rebuild at idle.
QW-12: preview release gate. Acceptance: affected-device retest, A/B update with an old tab, both original and new offline paths, no data loss when rolling assets back.
